# Gradle
.gradle/
build/
local.properties

# Signing. The keystore and its passwords must never be committed: anyone holding
# them can publish an update that Android will install over a user's existing
# oneMind. See RELEASING.md.
keystore.properties
*.jks
*.keystore

# IDE
.idea/
*.iml
.DS_Store

# Android
app/release/
app/debug/
*.apk
*.aab
*.sha256

# Kotlin
*.class

# Generated
# NOTE: app/schemas/ is deliberately NOT ignored. Room exports schema JSON there
# and migration tests verify against the schema of the version they migrate from,
# so those files are source, not build output.
