<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuration for the screen-capture accessibility service.

  canRetrieveWindowContent is false: we never read the screen's text or node tree.
  The only information this service uses is which window is focused (to know which
  app the user was in), and the takeScreenshot() call — both of which are available
  without window content access.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:canTakeScreenshot="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity="com.onemind.app.ui.settings.SettingsActivity" />
